# M12.4 — ROM-start ASM promotion `0x000000..0x0007C4`

Result: `M12_4_ROM_START_PROMOTION_PARTIAL_EXACT`

Baseline: `3e667c304382c5bce81d2e5a4ff75751139db314`.
Canonical ROM: 3,145,728 bytes, CRC32 `C4728225`, SHA1
`2944910c07c02eace98c17d78d07bef7859d386a`, SHA256
`eb19bda4982366a2fd43d65ab8a7f9709d83a8cc902c14a682c088c16359c263`.

## Scope and ownership

The 1,988-byte ROM-start region was treated as a mixed vector/header/startup
area. Vector entries are not instructions. No native C++ gameplay/runtime work,
portable-subsystem migration, or emulator expansion was started.

| interval | ownership | bytes | evidence |
| --- | --- | ---: | --- |
| `0x000000..0x000100` | `HEADER_VECTOR_ASM` | 256 | 64 fixed longwords; reset target `0x00020E`; ROM/RAM/other target audit |
| `0x000100..0x000200` | `HEADER_VECTOR_ASM` | 256 | fixed Genesis header layout; explicit `dc.b`; no text normalization |
| `0x000200..0x00020E` | `68000_CODE_CONFIRMED` | 14 | vector-referenced exception stubs and terminal `RTE` instructions |
| `0x00020E..0x00029C` | `68000_CODE_CONFIRMED` | 142 | reset-vector entry, three static xrefs, observed startup CFG |
| `0x00029C..0x000308` | `STRUCTURED_DATA_CONFIRMED` | 108 | PC-relative table consumed by exact `MOVEM` and postincrement loads |
| `0x000308..0x00045A` | `68000_CODE_CONFIRMED` | 338 | bounded startup CFG; stops before unresolved indirect `JSR (A1)` |
| `0x0006F6..0x0007C4` | `68000_CODE_CONFIRMED` | 206 | three static callers and bounded helper CFG meeting trusted ASM at `0x0007C4` |

The exact code ranges are `0x000200..0x000204`, `0x000204..0x000206`,
`0x000206..0x00020A`, `0x00020A..0x00020C`, `0x00020C..0x00020E`,
`0x00020E..0x00029C`, `0x000308..0x00045A`, and `0x0006F6..0x0007C4`.
The two adjacent `RTE` words are intentionally separate terminal ranges.

The range tool uses normal address labels and vasm flags
`-m68000 -no-opt -Fbin`. The opcode `MOVE USP` is retained as exact `dc.w`
because this vasm build emits the opposite `0x4E6x` encoding for its USP
operand spelling; this is the documented, byte-preserving fallback.

## Target metrics

| metric | before | after |
| --- | ---: | ---: |
| `HEADER_VECTOR_ASM_BYTES` | 0 | 512 |
| `68000 ASM bytes` | 0 | 700 |
| `STRUCTURED_DATA_ASM_BYTES` | 0 | 108 |
| `PADDING_ALIGNMENT_BYTES` | 0 | 0 |
| remaining blob bytes | 1,988 | 668 |
| `POSSIBLE_CODE_BYTES` | 0 | 560 |
| `UNKNOWN_DATA_BYTES` | 0 | 104 |
| `UNRESOLVED_BOUNDARY_BYTES` | 1,988 | 4 |
| `EXECUTABLE_BLOB_BYTES_REMOVED` | 0 | 700 |
| `NONEXECUTABLE_BLOB_BYTES_REMOVED` | 0 | 620 |

The before-side unresolved count is the conservative baseline blob census; the
after-side count is split by the M12.4 evidence classes below.

Remaining target intervals:

| interval | class | bytes | reason |
| --- | --- | ---: | --- |
| `0x00045A..0x00045E` | `UNRESOLVED_BOUNDARY` | 4 | indirect `JSR (A1)` at `0x00045A` has no proven transfer ownership |
| `0x00045E..0x0004C6` | `UNKNOWN_DATA` | 104 | indexed dispatch/table and string mix; only partial layout is proven |
| `0x0004C6..0x0006F6` | `POSSIBLE_CODE` | 560 | code-like island lacks observed, vector, or trusted static entry |

## Whole-ROM census after materialization

| ownership | ranges | bytes | percentage of ROM |
| --- | ---: | ---: | ---: |
| 68000 ASM | 244 | 15,858 | 0.5041122436523438% |
| `HEADER_VECTOR_ASM` | 2 | 512 | 0.016276041666666668% |
| `STRUCTURED_DATA_ASM` | 1 | 108 | 0.0034332275390625% |
| `PADDING_ALIGNMENT` | 0 | 0 | 0% |
| blob | 147 | 3,129,250 | 99.47617848714192% |

Source-owned total is 16,478 bytes, or `0.5238215128580729%`; this is kept
separate from dynamic execution coverage. The materialized manifest has zero
gaps and zero overlaps. Full-ROM byte identity is exact; rebuilt CRC32, SHA1,
and SHA256 equal the canonical values above.

## Recomputed M12.5 queue

Exactly one proposal is recorded and not started:
`0x003B3E..0x004A92` — 3,924 bytes, 12 observed PCs, 3 static xrefs. It is
the largest remaining source-owned byte-gain opportunity in the recomputed
queue. M12.5 remains outside this task.

## Verification

The transaction performs per-range vasm round trips, full-ROM materialization,
manifest coverage checks, and canonical hash checks. Both Debug and Release
CTest pass all 78 tests. Debug, Release, and GNU-equivalent MinGW builds link
successfully. The independent evidence audit reports full-ROM exactness and
the expected 239 round-trip-exact plus 5 statically supported code records;
the separate manifest/source audit confirms target partitioning, non-target
preservation, explicit directives, and the canonical rebuilt hash.
`git diff --check`, the source-size policy, and tracked-artifact hygiene pass.
